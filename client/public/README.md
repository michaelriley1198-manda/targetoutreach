# Static assets

Drop `hold-music.mp3` here — a short royalty-free loop served at `/hold-music.mp3`
by Vite. The browser dialer plays it while a call is connecting until AMD
classifies the answer as HUMAN (then it stops and the bridge audio unmutes) or
MACHINE (then it stops and the UI advances to the next lead).
